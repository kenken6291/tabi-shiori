# 構築・デプロイ手順

## ステップ1: スプレッドシート & GAS の準備

1. Google スプレッドシートを新規作成する（シート名やファイル名は自由）。
2. メニューから「拡張機能」→「Apps Script」を開く。
3. デフォルトで開かれる `コード.gs`（または `Code.gs`）の中身を全て削除し、`Code.gs` の内容を貼り付けて保存する（Ctrl+S）。
   - スプレッドシートに紐付いたスクリプト（コンテナバインド型）なので、`SHEET_ID` は空文字のままでOKです。
   - 別のスプレッドシートを使いたい場合のみ、`SHEET_ID` にそのスプレッドシートのIDを設定してください。
4. 一度、エディタ上部の「実行」ボタンで `doGet` を手動実行し、権限承認のダイアログを通しておく（初回のみ必要）。
   - 「このアプリは Google で確認されていません」と出た場合は「詳細」→「（プロジェクト名）に移動」→「許可」を選択。
   - この操作により、`Itineraries` シートと `bookmark_id / title / dates / timeline / items / updated_at` のヘッダー行が自動生成されます。
5. AIによるリスト作成支援（✨AIにおまかせ）を使う場合は、[Google AI Studio](https://aistudio.google.com/apikey) 等でGemini APIキーを取得し、Apps Scriptエディタの左メニュー「プロジェクトの設定」（歯車アイコン）→「スクリプト プロパティ」で以下を追加してください。
   - プロパティ: `GEMINI_API_KEY`
   - 値: 取得したAPIキー
   - ※ 未設定の場合、タイムライン・持ち物リストの「✨AIにおまかせ」ボタンはエラーメッセージを表示します（しおり自体の作成・共有・保存機能には影響ありません）。

## ステップ2: Web アプリとしてデプロイ

1. Apps Script エディタ右上の「デプロイ」→「新しいデプロイ」を選択。
2. 種類の選択（歯車アイコン）で「ウェブアプリ」を選ぶ。
3. 設定は以下の通り:
   - 説明: 任意（例：「旅のしおり API v1」）
   - **次のユーザーとして実行**: 自分（Me）
   - **アクセスできるユーザー**: 全員（Anyone）
     ※ 「全員」にしないと、URLを知らない第三者どころかフロントエンド自体からもアクセスできません。
4. 「デプロイ」をクリックし、表示された **ウェブアプリのURL**（`https://script.google.com/macros/s/xxxxx/exec` の形式）をコピーする。
5. 以後、`Code.gs` を修正した場合は「デプロイ」→「デプロイを管理」→ 編集(鉛筆アイコン)→ バージョンを「新バージョン」にして「デプロイ」し直す必要があります（URLは変わりません）。

## ステップ3: フロントエンドの設定と公開

1. `index.html` の冒頭付近にある以下の行を、ステップ2で取得したURLに書き換える。

   ```js
   const GAS_API_URL = 'https://script.google.com/macros/s/XXXXXXXXXXXXXXXXXXXXXXXX/exec';
   ```

2. GitHub にリポジトリを作成し、`index.html` をプッシュする。
3. リポジトリの「Settings」→「Pages」で、Source を `Deploy from a branch`、Branch を `main` / `/(root)` に設定して保存する。
4. 数分後、`https://<ユーザー名>.github.io/<リポジトリ名>/` でアクセス可能になる。
   - Vercel を使う場合は、リポジトリを import するだけで自動デプロイされます（ビルド設定不要の静的サイトとして認識されます）。

## 動作確認

1. 公開したURL（例: `https://xxxx.github.io/travel-note/`）にアクセスし、「＋ 新しいしおりを作成」を押す。
2. `?id=xxxxxxxx-xxxx-...` が付与されたURLに遷移することを確認する。
3. タイトルや持ち物を入力し、1〜2秒待って画面右下に「保存済み」と表示されることを確認する。
4. スプレッドシートを開き、`Itineraries` シートに1行データが追加されていることを確認する。
5. 別のブラウザ（またはシークレットウィンドウ）で同じURLを開き、同じ内容が表示されること（＝共同編集できること）を確認する。
6. タイトルを入力した状態で「🕒 タイムライン」「🎒 持ち物リスト」それぞれの「✨ AIにおまかせ」を押し、数秒後に提案が追加されること（GEMINI_API_KEY未設定の場合はエラーメッセージが出ること）を確認する。

## 過去の登録の再表示・修正・削除について

- このアプリはログイン不要のため、「自分が過去に作ったしおり一覧」をサーバー側では管理していません。
- 代わりに、しおりを開くたび（新規作成時・URLアクセス時）に、そのブラウザの `localStorage` へタイトル・日程・IDを記録しています。トップページ（`id`なしでアクセスしたとき）にその履歴が一覧表示され、「開く」で再表示・修正、「削除」でそのしおり自体を削除できます。
- 履歴はブラウザ単位（端末・ブラウザごと）です。別のブラウザや別端末からは見えません。シークレット/プライベートモードでは保存されません。
- しおり編集画面の下部にある「🗑 このしおりを削除する」ボタンからも、今開いているしおりをいつでも削除できます。
- 削除は元に戻せません（スプレッドシートの該当行を直接削除します）。

この機能追加により `Code.gs` を更新した場合は、ステップ2の手順5の通り「デプロイを管理」→ 新バージョンとして再デプロイしてください（URLは変わりません）。

## 注意点・既知の制約

- **リアルタイム同期ではありません**。他の人の変更は自分がリロードするまで画面に反映されません（要件通り「リロード時」の同期）。
- 同時に同じ項目を編集すると、後から保存した方の内容で上書きされます（`LockService` により行の破損は防いでいますが、楽観ロックはありません）。
- GAS の Web アプリは CORS のプリフライト（OPTIONS）に対応できないため、`index.html` 側は `Content-Type: text/plain` でPOSTしています。この部分は変更しないでください。
- URLを知っている人は誰でも閲覧・編集できる設計です。機密情報を書き込まないようご注意ください。