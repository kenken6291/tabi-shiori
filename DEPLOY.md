# 構築・デプロイ手順

## ステップ1: スプレッドシート & GAS の準備

1. Google スプレッドシートを新規作成する（シート名やファイル名は自由）。
2. メニューから「拡張機能」→「Apps Script」を開く。
3. デフォルトで開かれる `コード.gs`（または `Code.gs`）の中身を全て削除し、`Code.gs` の内容を貼り付けて保存する（Ctrl+S）。
   - スプレッドシートに紐付いたスクリプト（コンテナバインド型）なので、`SHEET_ID` は空文字のままでOKです。
   - 別のスプレッドシートを使いたい場合のみ、`SHEET_ID` にそのスプレッドシートのIDを設定してください。
4. 一度、エディタ上部の「実行」ボタンで `doGet` を手動実行し、権限承認のダイアログを通しておく（初回のみ必要）。
   - 「このアプリは Google で確認されていません」と出た場合は「詳細」→「（プロジェクト名）に移動」→「許可」を選択。
   - この操作により、`Itineraries` シートと `bookmark_id / title / dates / timeline / items / updated_at / owner_id / shared_with` のヘッダー行が自動生成されます。
5. AIによるリスト作成支援（✨AIにおまかせ）を使う場合は、[Google AI Studio](https://aistudio.google.com/apikey) 等でGemini APIキーを取得し、Apps Scriptエディタの左メニュー「プロジェクトの設定」（歯車アイコン）→「スクリプト プロパティ」で以下を追加してください。
   - プロパティ: `GEMINI_API_KEY`
   - 値: 取得したAPIキー
   - ※ 未設定の場合、タイムライン・持ち物リストの「✨AIにおまかせ」ボタンはエラーメッセージを表示します（しおり自体の作成・共有・保存機能には影響ありません）。
6. 会員登録機能を使うには（※現在の仕様では、しおりの作成・編集・削除に会員ログインが必須なので、実質必ず設定してください）、同じ「スクリプト プロパティ」に以下も追加してください。
   - プロパティ: `AUTH_PEPPER`
   - 値: 他人に推測されない適当な長さのランダム文字列（例：長めのパスワード生成ツールで作った32文字程度の文字列）。パスワードのハッシュ化に使う秘密の値で、他の用途に使い回さないでください。
   - ※ 未設定のまま会員登録・ログインを行うとエラーになります。ログインできないと、しおりの新規作成・編集・削除も一切できなくなるのでご注意ください（閲覧のみは引き続き可能です）。
7. 会員登録・パスワード再発行の際、登録メールアドレスへ仮パスワードを送信します。これはGoogleアカウントの `MailApp`（Gmail送信）を利用するため、追加設定は不要です。ただし個人のGoogleアカウントでは1日あたりの送信数に上限（通常100通程度）があるのでご注意ください。

## ステップ2: Web アプリとしてデプロイ

1. Apps Script エディタ右上の「デプロイ」→「新しいデプロイ」を選択。
2. 種類の選択（歯車アイコン）で「ウェブアプリ」を選ぶ。
3. 設定は以下の通り:
   - 説明: 任意（例：「旅のしおり API v1」）
   - **次のユーザーとして実行**: 自分（Me）
   - **アクセスできるユーザー**: 全員（Anyone）
     ※ 「全員」にしないと、URLを知らない第三者どころかフロントエンド自体からもアクセスできません。
4. 「デプロイ」をクリックし、表示された **ウェブアプリのURL**（`https://script.google.com/macros/s/xxxxx/exec` の形式）をコピーする。
5. 以後、`Code.gs` を修正した場合は「デプロイ」→「デプロイを管理」→ 編集(鉛筆アイコン)→ バージョンを「新バージョン」にして「デプロイ」し直す必要があります（URLは変わりません）。

## ステップ3: フロントエンドの設定と公開

1. `index.html` の冒頭付近にある以下の行を、ステップ2で取得したURLに書き換える。

   ```js
   const GAS_API_URL = 'https://script.google.com/macros/s/XXXXXXXXXXXXXXXXXXXXXXXX/exec';
   ```

2. GitHub にリポジトリを作成し、`index.html` をプッシュする。
3. リポジトリの「Settings」→「Pages」で、Source を `Deploy from a branch`、Branch を `main` / `/(root)` に設定して保存する。
4. 数分後、`https://<ユーザー名>.github.io/<リポジトリ名>/` でアクセス可能になる。
   - Vercel を使う場合は、リポジトリを import するだけで自動デプロイされます（ビルド設定不要の静的サイトとして認識されます）。

## 動作確認

1. 公開したURL（例: `https://xxxx.github.io/travel-note/`）にアクセスし、右上の「会員登録」から自分のアカウントを作る（ニックネーム＋メールアドレスのみ）。届いた仮パスワードでログインし、その場で新しいパスワードに変更する。
2. ログイン後、「＋ 新しいしおりを作成」を押す。`?id=xxxxxxxx-xxxx-...` が付与されたURLに遷移することを確認する。
3. タイトルや持ち物を入力し、1〜2秒待って画面右下に「保存済み」と表示されることを確認する。
4. スプレッドシートを開き、`Itineraries` シートに1行データが追加され、`owner_id` 列に自分の会員IDが入っていることを確認する。
5. 別のブラウザ（またはシークレットウィンドウ、ログインなし）で同じURLを開き、内容は表示されるが（閲覧のみ）入力欄が操作できず「🔒 閲覧のみです」と表示されることを確認する。
6. 「📧 公開相手」にメールアドレスを追加し、「✉️ 登録した相手にURLを送信」を押す。宛先にメールが届き、本文のURLからログインなしで閲覧できることを確認する。
7. タイトルを入力した状態で「🕒 タイムライン」「🎒 持ち物リスト」それぞれの「✨ AIにおまかせ」を押し、数秒後に提案が追加されること（GEMINI_API_KEY未設定の場合はエラーメッセージが出ること）を確認する。

## 会員登録機能とアクセス権限について

- 画面右上に「ログイン」「会員登録」リンクが表示されます。会員登録はニックネームとメールアドレスのみで行え、登録すると仮パスワードがメールで送信されます。
- 仮パスワードでログインすると、その場でパスワード変更が求められます（強制）。
- ログイン中は右上に「パスワード変更」「ログアウト」が表示されます。パスワードを忘れた場合はログイン画面の「パスワードを忘れた方」から再発行できます。
- ログイン試行が同一メールアドレスで5回連続失敗すると15分間ロックされます。
- 会員情報は新設の `Members` シートに保存されます（`Itineraries` シートとは別）。パスワードは平文では保存せず、SHA-256（ソルト＋ペッパー付き）でハッシュ化して保存します。

### アクセス権限（重要な仕様変更）

- **しおりの新規作成・編集（タイトル/日程/タイムライン/持ち物の変更）・削除には、会員ログインが必須です。** 未ログインの状態で新規のURLにアクセスすると「作成するには会員ログインが必要です」という案内が表示され、作成できません。
- **しおりの閲覧はログイン不要です。** URLを知っていれば誰でも見られます。作成者本人以外がURLを開いた場合は「🔒 閲覧のみです」という表示になり、入力欄は操作できません（削除ボタンや「＋追加」ボタンも表示されません）。
- しおりには `owner_id`（作成者の会員ID）が記録され、以後の編集・削除はその本人（同じ会員としてログインしている場合）のみ許可されます。他の会員がログインしていても、自分が作成していないしおりは編集できません。
- 会員機能追加前（未ログイン時代）に作成された既存のしおりは `owner_id` が空です。ログイン中の会員が最初に保存した時点で、その会員がそのしおりの所有者になります（早い者勝ちで所有者が決まります）。
- トップページの「🗂 マイしおり」から、ログイン中の会員がどの端末・ブラウザからでも自分が作成したしおりの一覧を見ることができます（ログインしていないブラウザにも残る「📖 過去に開いたしおり」の履歴とは別物です）。

### 公開相手（招待メール送信）

- しおり編集画面に「📧 公開相手」欄があり、メールアドレスを登録できます（作成者のみ操作可能、最大30件）。
- 「✉️ 登録した相手にURLを送信」を押すと、登録したメールアドレス宛にしおりのURLがメールで送信されます（Gmailの送信数上限にご注意ください。上記ステップ1の7参照）。
- 送信された相手は、そのURLからログインなしで閲覧できます（編集はできません）。
- 公開相手のリストはしおりごとに保存され、単なる「メール送信先の管理」用です。URLを知っている人なら公開相手として登録していなくても閲覧はできる点は変わりません（アクセス制御ではなく、招待の記録・再送用の機能です）。

この機能追加に伴い `Code.gs` を更新した場合は、ステップ2の手順5の通り「デプロイを管理」→ 新バージョンとして再デプロイしてください（URLは変わりません）。また、上記ステップ1の6にある `AUTH_PEPPER` の設定を忘れずに行ってください。

## 過去に開いたしおりの再表示について（ブラウザ履歴）

- 「🗂 マイしおり」（会員ログイン時、サーバー側）とは別に、しおりを開くたび（新規作成時・URLアクセス時）に、そのブラウザの `localStorage` へタイトル・日程・IDを記録する機能もあります。トップページ（`id`なしでアクセスしたとき）に「📖 過去に開いたしおり」として一覧表示され、「開く」で再表示できます。
- 履歴はブラウザ単位（端末・ブラウザごと）です。別のブラウザや別端末からは見えません。シークレット/プライベートモードでは保存されません。
- 履歴一覧やしおり編集画面から削除を試みることはできますが、上記の通り削除には作成者としてのログインが必要なため、作成者本人でログインしていない場合はエラーになります（権限エラーの場合、履歴からは削除されません）。
- 削除は元に戻せません（スプレッドシートの該当行を直接削除します）。

## 注意点・既知の制約

- **リアルタイム同期ではありません**。他の人の変更は自分がリロードするまで画面に反映されません（要件通り「リロード時」の同期）。
- 同時に同じ項目を編集すると、後から保存した方の内容で上書きされます（`LockService` により行の破損は防いでいますが、楽観ロックはありません）。
- GAS の Web アプリは CORS のプリフライト（OPTIONS）に対応できないため、`index.html` 側は `Content-Type: text/plain` でPOSTしています。この部分は変更しないでください。
- **URLを知っている人は誰でも閲覧できます**が、編集・削除は作成者本人のログインが必要です。閲覧用URLであっても機密情報を書き込まないようご注意ください。